# 핵심 파이프라인은 표준 라이브러리만으로 동작한다(수집/스코어링/export/테스트).
# 아래는 선택 의존성:
python-dotenv>=1.0     # .env 로딩 (없으면 환경변수만 사용)
pytest>=8.0            # 테스트
# requests>=2.31       # (선택) 크롤러를 requests 기반으로 교체할 때
# playwright>=1.44      # (선택) JS 렌더가 필요한 사이트 크롤링 시
